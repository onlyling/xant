// import normalizeText from './normalize-text';
// import normalizeSize from './normalize-size';
import hex2rgba from './hex2rgba';
import { getNextZIndex } from './z-index';

export default {
  // normalizeText,
  // normalizeSize,
  hex2rgba,
  getNextZIndex,
};
